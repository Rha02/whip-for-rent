interface Car {
    id: string;
    make: string;
    model: string;
    year: number;
    color: string;
    price: number;
    image_url: string;
    createdAt: Date;
    updatedAt: Date;
}

export default Car;
